{
  "name": "sizem-campo",
  "version": "0.1.0",
  "private": true,
  "description": "Shell Android (Capacitor) do SIZEM — ação em campo",
  "scripts": {
    "sync": "cap sync android",
    "sync:local": "SIZEM_ALVO=local cap sync android",
    "apk": "cd android && ./gradlew assembleDebug",
    "apk:local": "npm run sync:local && cd android && ./gradlew assembleDebug",
    "apk:producao": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/splash-screen": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "typescript": "^5.9.3"
  }
}
