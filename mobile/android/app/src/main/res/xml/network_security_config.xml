<?xml version="1.0" encoding="utf-8" ?>
<!--
    A partir do Android 9 (API 28) trafego em claro e bloqueado por padrao, o que
    impede o build de desenvolvimento de carregar o servidor local por HTTP.

    Em vez de liberar cleartext no app inteiro (android:usesCleartextTraffic),
    a excecao fica restrita aos enderecos de desenvolvimento: 10.0.2.2 e como o
    emulador enxerga o host, e localhost cobre o encaminhamento por adb reverse.
    Producao continua obrigatoriamente em HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
